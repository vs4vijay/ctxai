[tool.poetry]
name = "codepilot"
version = "0.1.0"
description = ""
authors = ["vs4vijay <vs4vijay@gmail.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
fastapi = "^0.103.2"
pydantic = "^2.4.2"
python-dotenv = "^1.0.0"

[tool.poetry.scripts]
codepilot = "codepilot.__main__:main"

[tool.poetry.group.dev.dependencies]
ruff = "^0.0.292"
pytest = "^7.4.2"


[tool.poetry.group.server.dependencies]
uvicorn = "^0.23.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
